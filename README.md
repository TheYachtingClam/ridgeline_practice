## ridgeline_practice

Small Python practice repo for investment-industry coding + architecture interview prep.

## Setup

```bash
python -m venv .venv
. .venv/bin/activate
python -m pip install -U pip
pip install -e .
```

## Exercises

- `day1/sector-exposure-idempotent-trades.md`: multi-step position + FX + aggregation exercise
